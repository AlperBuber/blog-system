<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <style>
        .buttons {
            margin-left: 40px;
            bottom: 20px;
            right: 20px;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .buttons button {
            width: 100px;
        }
    </style>
    <title>Blogs</title>
</head>
<body>
    <nav>
       <h4> Blog System</h4>
       <ul>

        <li><a href="/">Home</a></li>
        <li><a href="/blogs">Blogs</a></li>
        <li><a href="/blog">Add Blog</a></li>
        <li><a href="/login">Login</a></li>
        <li><a href="/register">Register</a></li>
       </ul>


    </nav>


    <div class="container mt-4">
        <h1 class="mb-4"><%= blog.title %></h1>
        <p><%= blog.content %></p>
    </div>
    <div class="comments-section m-4">
        <h3>Yorumlar</h3>
        <% if (comments.length > 0) { %>
            <ul class="list-group">
                <% comments.forEach(comment => { %>
                    <li class="list-group-item">
                        <strong>Kullanıcı ID: <%= comment.users_id %></strong><br>
                        <%= comment.content %>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>Henüz yorum yapılmamış.</p>
        <% } %>
    </div>
    
<form action="/blogs/<%= blog.id %>/comments" method="POST" class="mt-4">
    <div class="comment">
        <label for="comment">Comment</label>
        <textarea class="form-control" id="comment" name="comment" rows="3"></textarea>
      </div>
    <div class="buttons">
        <button class="btn btn-primary">Yorum Yap</button>
        
    </div>
</form>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
